pipeline {
  agent any
  stages {
    stage('compile') {
      steps {
        bat 'echo this is compile step'
      }
    }
    stage('unittest') {
      steps {
        bat 'echo this is unittest'
      }
    }
  }
}